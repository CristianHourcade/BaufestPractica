import { CalculeAgePipe } from './calcule-age.pipe';

describe('CalculeAgePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculeAgePipe();
    expect(pipe).toBeTruthy();
  });
});
